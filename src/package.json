{
  "name": "deskthing-gmp",
  "version": "0.7.0",
  "description": "GMP (Global Media Player) is a multiple streaming platform DeskThing app that locally controls playback of your selected streaming platform.",
  "main": "index.js",
  "scripts": {
    "build_package": "esbuild index.js --bundle --platform=node --outfile=builds/index.js && node buildHelper.js",
    "build": "del /F /Q builds latest\\GMP.zip 2>nul && npm run build_package && cd builds && ..\\..\\7z\\7z.exe a ../latest/GMP.zip * && cd .."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "0.21.5"
  },
  "dependencies": {
    "dbus-native": "^0.4.0",
    "dbus-next": "^0.10.2",
    "deskthing-client": "^0.8.1",
    "deskthing-server": "^0.8.0",
    "os": "^0.1.2",
    "readline": "^1.3.0",
    "x11": "^2.3.0"
  }
}
